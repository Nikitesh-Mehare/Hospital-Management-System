# Build output (regenerated by Clean and Build) — keep dist/hms.war though, Docker needs it
/build/

# NetBeans machine-specific / private settings (contains local paths, not portable)
/nbproject/private/

# OS / IDE junk
.DS_Store
Thumbs.db
*.log
